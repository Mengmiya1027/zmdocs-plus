---
wiki: xtc-root
title: 基础概念
author: Mengmiya1027
references:
  - '[AllToolBox官方网站 新手必看 基础词语 by Appluuicp2233](https://www.alltoolbox.live/docs/view.php?uid=850024946640)'
---

# 基础概念

在开始动手 root 之前，先了解一下这些基础概念与缩写。

## 概念

### ADB

Android Debug Bridge，简称 adb，在 Android 开发者文档中是这么描述它的：

> adb 是一种多功能命令行工具，可让您与设备进行通信。该 adb 命令有助于各种设备操作，例如安装和调试应用程序。adb 提供对 Unix shell 的访问，您可以使用它在设备上运行各种命令。它是一个客户端-服务器程序。

这听起来有些难以理解——您也没有必要完全理解它。如果您对本文中的任何关键名词产生疑惑或兴趣，都可以在搜索引擎中去搜索它。简单来说：adb 是一款在命令行中运行的、用于对 Android 设备进行调试的工具，拥有比一般用户及程序更高的权限，因此我们可以使用它对 Android 设备进行最基本的调试操作。

要在小天才电话手表上启用它，您只需要这么做：

1. 打开拨号盘；
2. 输入 `*#0769651#*`；
3. 点按打开 adb 调试选项。

{% note color:cyan 机型差异 对于 Z6 级以下机型，可按照上述步骤操作；Z6 及以上机型，请查看[此教程](https://alltoolbox.live/docs/video.php?uid=473324438013)。 %}

### Root

在 Android 系统中，Root 是指获取系统的最高管理员权限（类似于 Windows 的 Administrator 或 Linux 的 root 用户）。默认情况下，普通用户和应用程序只能访问自身沙盒内的文件与系统资源，无法修改系统核心文件或执行高权限操作。而获取 Root 权限后，您可以绕过这些限制，实现诸如卸载预装应用、修改系统配置文件、运行需要深层访问的调试工具，甚至刷入第三方固件等操作。

不过需要说明的是，Root 并非系统官方开放的功能，通常需要通过特定的漏洞或解锁引导加载程序（Bootloader）来实现，操作不当可能导致设备变砖、失去保修或引入安全风险。因此，除非您明确知晓自己的操作目的并愿意承担相应风险，否则不建议轻易尝试。

### 9008

**高通手机芯片的「紧急下载模式」（Emergency Download Mode，简称 {% mark EDL %}）**，它因电脑上显示的端口名 "Qualcomm HS-USB QDLoader 9008" 而得名。

这是手表变砖（无法开机、无法进入 Recovery 或 Fastboot 模式）后的终极「救砖」手段。

- **深度底层访问**：它比常规的 Recovery 或 Fastboot 模式更底层，独立于安卓系统之外。
- **全分区读写**：在此模式下，可以读写手机存储的**所有分区和数据**。
- **核心应用场景**：包括**超级恢复**、{% mark OTA %} 和**备份**。

### OTA

在 XTC root 中所能遇到的 OTA 操作，大部分为「离线 OTA」。

离线 OTA 是指设备通过 USB、U 盘、蓝牙等非互联网方式获取升级包，然后在本地完成安装的系统更新过程。它和传统的「刷机」在操作上很像，但核心区别在于它更新的是系统的一部分，而非完整覆盖。

如果你需要对手表 OTA，需要准备对应版本的 OTA 包（通常为 `.zip` 文件），然后：

1. 打开 ATB，选择「常用功能」，进入「离线 OTA」；
2. 连接电脑与手表；
3. 在弹出的选择窗口中选择对应的 OTA 包，等待即可。

### Fastboot（线刷协议）

安卓 Bootloader 阶段运行的线刷协议与命令行工具，用于 PC 通过 USB 对设备分区进行底层读写操作，实现镜像烧录、引导锁管理及分区修复等工程目标。

{% folding Root 前条件 open:true color:yellow %}
- 引导锁状态（{% mark Lock %}/{% mark Unlock %}）直接决定系统分区是否可写，Root 操作须先通过 Fastboot 执行解锁命令，此为前置必要条件。
- 解锁操作伴随用户分区（`userdata`）格式化，个人数据在解锁指令触发时即被清空，须提前完成离线备份。
- 不同厂商对解锁策略存在差异（如小米需申请、华为已收紧），具体流程须对照机型独立查阅，本文不展开。
- 刷入镜像须与机型硬件及固件版本严格匹配，写入中断或文件不匹配可能造成设备无法引导（即「变砖」）。
{% endfolding %}

### Bootloader（启动引导程序）

设备开机后最先运行的底层引导程序，负责初始化硬件、校验系统完整性，并决定加载哪个系统镜像启动。

**与 Fastboot 的关系：** Bootloader 处于 Fastboot 上游，Fastboot 的运行依赖于 Bootloader 的底层支持。

{% tabs active:1 %}
<!-- tab 锁定（Locked） -->
仅允许加载官方签名的系统镜像，禁止对系统分区进行写入操作。
<!-- tab 解锁（Unlocked） -->
允许通过 Fastboot 协议向系统分区写入镜像，是获取 Root 权限及安装第三方系统的前置条件。
{% endtabs %}

### Magisk（面具）

当前主流的 Root 权限管理工具及模块化框架，通过无系统修改（{% mark systemless %}）方式实现 Root，即在 `init_boot` 或 `boot` 分区挂载模块，保持系统分区原始状态，规避部分系统完整性检测（如 {% mark SafetyNet %}）。

#### 功能

管理 Root 授权，对请求 Root 权限的应用进行授予或拒绝；加载 Magisk 模块，可修改系统行为（如隐藏 Root、调整性能参数、启用系统级功能），无需改动系统文件。

#### 与 Fastboot 的关系

Fastboot 负责刷入修补后的 `init_boot`/`boot` 镜像，Magisk 运行于系统启动阶段，将模块挂载至系统，两者为串行关系：Fastboot 写入 → Magisk 生效。

### QMMI（高通全功能测试模式）

是高通平台设备内置的硬件自检工程模式，全称为 {% mark Qualcomm Manufacturing Mode Interface %}。

它作为系统级 APK 应用运行，提供屏幕、触摸、指纹、摄像头、传感器等数十项硬件功能的诊断与测试入口，主要用于工厂产线质检和售后维修场景。

在小天才手表上，QMMI 是绕过系统限制、进入硬件测试界面的后门通道。由于小天才系统通常屏蔽了拨号暗码，玩家需要通过 ADB 命令强制拉起 QMMI 主界面。进入后，通过在 QMMI 内部执行特定操作（如选择「重启到 QMMI 模式」再重启回来），可以触发系统 ADB 开关被重新激活，从而在手表开发者选项中被隐藏或置灰的「USB 调试」选项得以再次开启。这是目前安装第三方应用最主要的突破口。

### Recovery/TWRP（恢复模式）

第三方定制的安卓恢复环境，用于替代官方 Recovery，实现第三方 ROM 刷入、Root 权限获取、数据清除及系统备份等高级刷机操作。

#### 与 Fastboot 的关系

Fastboot 属于 Bootloader 阶段的线刷协议，烧录对象为分区镜像（如 `boot.img`、`system.img`）；Recovery 属于系统恢复阶段的交互式环境，刷入对象为完整 ROM 包（`.zip` 格式），两者为互补关系，非取代关系。

#### 条件

刷入 TWRP 须先通过 Fastboot 执行 `fastboot flash recovery twrp.img` 命令，前提为 Bootloader 处于解锁（Unlocked）状态。

#### 功能

TWRP 支持触屏交互，可执行文件管理、分区备份与还原、格式化特定分区（如 Data、Cache）等操作；官方 Recovery 通常仅支持 OTA 更新及基础数据清除，功能受限。

{% note color:yellow 提示 TWRP 刷入错误版本或操作不当将导致设备无法进入系统（变砖）；格式化 Data 分区将清空用户数据，须提前备份。 %}

## 此外的部分缩写

| 缩写   | 含义                                                        |
|:-------|:------------------------------------------------------------|
| ATB    | AllToolBox 工具箱。你可以点击左下角的链接来查看 ATB 官网。  |
| sysp   | System Plus                                                 |
| sysui  | System UI                                                   |
| 4100   | 骁龙 Wear4100 芯片，代指小天才 Z6-Z9                        |
| 5100   | 骁龙 Wear5100 芯片，代指小天才 Z10 及以上                   |
| SNB    | 少年版，如 `Z8SNB` 代指 Z8 少年版                           |
| y      | 代表 "`Yes`"                                                |


{% note 版本号通用缩写 去除版本号中间的 `.`<br>例如：Z10 `3.0.2` → Z10 `302`；桌面 `13.05.10` → 桌面 `130510`。 %}